<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juwanda.adata">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"
            android:label="Daftar Akun"/>
        <activity android:name=".MainActivity"
            android:label="Daftar AData">
        </activity>
        <activity android:name=".BuatData"
            android:label="Tambah Data">
        </activity>
        <activity android:name=".LihatData"
            android:label="Lihat Data">
        </activity>
        <activity android:name=".EditData"
            android:label="Perbaharui Data">
        </activity>
    </application>

</manifest>